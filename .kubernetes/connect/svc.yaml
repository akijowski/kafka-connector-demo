apiVersion: v1
kind: Service
metadata:
  labels:
    app: connect
  name: connect
spec:
  ports:
  - name: api
    nodePort: 30951
    port: 8083
    protocol: TCP
    targetPort: api
  selector:
    app: connect
  type: NodePort
